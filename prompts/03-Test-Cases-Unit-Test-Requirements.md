You are a QA lead + test architect.

From the PRD and sequence diagrams, produce:
• Comprehensive test case table (ID, Description, Preconditions, Steps, Expected Result, Priority) for happy path + edge cases.
• Unit test requirements matrix (which components/functions need unit tests, integration tests, E2E).
• Coverage goals (e.g. 80%+ for core logic).

Output in clean Markdown tables.

Then add to Project Knowledge and create a new documentation page "/docs/test-strategy".
