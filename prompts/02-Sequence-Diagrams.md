Using the PRD, user journeys and architecture in Project Knowledge, generate COMPLETE sequence diagrams for every major flow in [APP_NAME].

For each user story / critical path (auth, main feature, payment, admin, etc.) create a separate Mermaid sequenceDiagram.

Use proper actors, lifelines, notes, alt/opt/loop fragments, and realistic Supabase/edge-function calls.

Output:
1. One Mermaid code block per flow (title it clearly)
2. A combined overview Mermaid diagram

Lovable will render these automatically. Add all diagrams to Project Knowledge and also create a new page called "/docs/sequence-diagrams" with all diagrams embedded.
