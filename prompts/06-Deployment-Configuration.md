Set up production-ready deployment for [APP_NAME]:

- Lovable Cloud deployment settings (custom domain, environment variables)
- GitHub repository structure & branch strategy (main = Live, develop = Test)
- GitHub Actions workflows (build, test, deploy to Lovable + fallback to Vercel/Netlify)
- Test vs Live environments (Supabase/Lovable Cloud)
- Post-deploy checklist (monitoring, rollback, secrets)

Output:
• Step-by-step deployment guide
• .github/workflows/deploy.yml example
• Environment variable template

Then actually configure the deployment settings in this project and create a "/docs/deployment" page.
